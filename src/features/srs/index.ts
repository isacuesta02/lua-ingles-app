/**
 * API pública del feature `srs` (repetición espaciada).
 *
 * Todo lo que una ruta de `src/app` u otro feature necesite de aquí se exporta
 * en este archivo. Nadie debe importar rutas internas (`@/features/srs/lib/...`)
 * desde fuera del feature.
 */
export {};
